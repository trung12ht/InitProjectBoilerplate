import React from 'react'

const Bold = ({text}) => {

    return (
        < >
            <div style={{ fontWeight: 'bold' }}>
                {text}
            </div>
        </>
    )
}

export default Bold
